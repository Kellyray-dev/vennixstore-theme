<section class="vx-values color-{{ section.settings.color_scheme }} gradient">
  <div class="vx-values__inner page-width">
    <header class="vx-values__header">
      <div>
        {%- if section.settings.eyebrow != blank -%}
          <p class="vx-section-eyebrow">{{ section.settings.eyebrow | escape }}</p>
        {%- endif -%}
        {%- if section.settings.heading != blank -%}
          <h2 class="vx-values__heading">{{ section.settings.heading | escape }}</h2>
        {%- endif -%}
      </div>
      {%- if section.settings.text != blank -%}
        <div class="vx-values__intro rte">{{ section.settings.text }}</div>
      {%- endif -%}
    </header>

    <ol class="vx-values__grid" role="list">
      {%- for block in section.blocks -%}
        <li class="vx-values__card" {{ block.shopify_attributes }}>
          <span class="vx-values__number" aria-hidden="true">0{{ forloop.index }}</span>
          <div>
            <h3>{{ block.settings.title | escape }}</h3>
            <p>{{ block.settings.description | escape }}</p>
          </div>
        </li>
      {%- endfor -%}
    </ol>

    {%- if section.settings.link_label != blank -%}
      <a class="vx-values__link link" href="{{ section.settings.link | default: routes.all_products_collection_url }}">
        {{ section.settings.link_label | escape }}
        <span aria-hidden="true">→</span>
      </a>
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "Vennix values",
  "tag": "section",
  "class": "section section--vennix-values",
  "disabled_on": { "groups": ["header", "footer"] },
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "The Vennix edit" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Modern clothing & active essentials" },
    { "type": "richtext", "id": "text", "label": "Introduction", "default": "<p>Considered pieces for everyday movement — premium clothing and active essentials, curated with a clean, modern point of view.</p>" },
    { "type": "text", "id": "link_label", "label": "Link label", "default": "Shop the edit" },
    { "type": "url", "id": "link", "label": "Link" },
    { "type": "color_scheme", "id": "color_scheme", "label": "Color scheme", "default": "scheme-2" }
  ],
  "blocks": [
    {
      "type": "reason",
      "name": "Value",
      "limit": 3,
      "settings": [
        { "type": "text", "id": "title", "label": "Title", "default": "Designed for movement" },
        { "type": "textarea", "id": "description", "label": "Description", "default": "Premium pieces made to feel refined, versatile, and easy to wear every day." }
      ]
    }
  ],
  "presets": [
    {
      "name": "Vennix values",
      "blocks": [
        { "type": "reason", "settings": { "title": "Premium essentials", "description": "Elevated everyday pieces with a modern, fashion-first feel." } },
        { "type": "reason", "settings": { "title": "Clear product details", "description": "Straightforward information helps you choose with confidence." } },
        { "type": "reason", "settings": { "title": "Support when needed", "description": "Questions about an order or product? Our support team is ready to help." } }
      ]
    }
  ]
}
{% endschema %}
